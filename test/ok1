/* FONT MODERN */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

/* RESET GENERAL */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    background: radial-gradient(circle at top, #292929 0, #050505 60%);
    color: #f4f4f4;
    min-height: 100vh;

    /* ANIMAȚIE LA INTRAREA PE PAGINĂ */
    animation: pageFadeIn 0.5s ease-out;
}

/* ===== HEADER ===== */
.site-header {
    text-align: center;
    padding: 40px 20px 10px;
}

.header-inner h1 {
    font-size: 40px;
    font-weight: 700;
    letter-spacing: 1px;
}

.subtitle {
    margin-top: 6px;
    font-size: 16px;
    color: #c4c4c4;
}

/* ===== MAIN WRAP ===== */
.main-wrap {
    width: 100%;
    max-width: 1000px;
    margin: 0 auto 40px auto;
    padding: 0 20px 40px;
}

/* ===== INTRO BOX ===== */
.intro-box {
    background: rgba(255,255,255,0.06);
    border-radius: 20px;
    padding: 25px 30px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.6);
    text-align: center;
}

.intro-box h2 {
    font-size: 26px;
    margin-bottom: 15px;
    color: #ffdd99;
}

.intro-box p {
    font-size: 17px;
    line-height: 1.7;
    color: #e0e0e0;
}

.intro-box .lead {
    margin-top: 10px;
    font-weight: 600;
    color: #ffffff;
}

/* ===== MENIU – CARDURI-BUTOANE ===== */

/* scoatem orice stil default de link */
.menu-container a {
    text-decoration: none;
    color: inherit;
}

/* grid pentru carduri */
.menu-container {
    margin-top: 35px;
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
}

/* cardul-buton propriu-zis (este <a>) */
.menu-card {
    display: block;
    background: linear-gradient(135deg, #262626, #3a3a3a);
    border-radius: 18px;
    padding: 18px 20px;
    box-shadow: 0 8px 24px rgba(0,0,0,0.7);
    border: 1px solid rgba(255,255,255,0.08);
    transition: transform 0.25s ease, box-shadow 0.25s ease, background 0.25s ease;
}

.menu-card h3 {
    font-size: 20px;
    margin-bottom: 8px;
    color: #ffdd99;
}

.menu-card p {
    font-size: 15px;
    color: #d0d0d0;
}

/* efect hover – se ridică și luminează */
.menu-card:hover {
    transform: translateY(-6px) scale(1.02);
    box-shadow: 0 14px 30px rgba(0,0,0,0.85);
    background: linear-gradient(135deg, #3f3f3f, #555555);
}

/* ===== CARD „DEPENDENȚA DE INTERNET” CENTRAT ===== */
/* ultimul card din .menu-container (Internet) */
.menu-container a.menu-card:last-child {
    grid-column: span 2;      /* ocupă ambele coloane */
    justify-self: center;     /* centrat pe rând */
    max-width: 500px;         /* aceeași lățime ca ceilalți */
}

/* ===== BUTON „AM NEVOIE DE AJUTOR” ===== */
.help-wrap {
    text-align: center;
    margin-top: 30px;
}

.help-btn {
    display: inline-block;
    padding: 14px 30px;
    border-radius: 999px;
    font-size: 18px;
    font-weight: 700;
    text-decoration: none;
    color: #ffffff;
    background: linear-gradient(135deg, #b00000, #ff2222);
    border: 1px solid rgba(255,80,80,0.7);
    box-shadow: 0 10px 26px rgba(255,0,0,0.45);
    transition: transform 0.22s ease, box-shadow 0.22s ease, background 0.22s ease;
}

.help-btn:hover {
    transform: translateY(-4px) scale(1.04);
    background: linear-gradient(135deg, #ff3333, #ff5555);
    box-shadow: 0 14px 32px rgba(255,0,0,0.6);
}

/* ===== FOOTER ===== */
.site-footer {
    text-align: center;
    padding: 10px 10px 25px;
    font-size: 13px;
    color: #a8a8a8;
}

/* ===== ANIMAȚII TRANZIȚIE PAGINI ===== */

@keyframes pageFadeIn {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes pageFadeOut {
    from {
        opacity: 1;
        transform: translateY(0);
    }
    to {
        opacity: 0;
        transform: translateY(10px);
    }
}

body.page-fade-out {
    animation: pageFadeOut 0.4s ease-in forwards;
}

/* ===== RESPONSIVE ===== */
@media (min-width: 720px) {
    .menu-container {
        grid-template-columns: repeat(2, 1fr);
    }
}
